apiVersion: v1
kind: Namespace
metadata:
  name: legend-prod
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: legend-studio
  namespace: legend-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: legend-studio
  template:
    metadata:
      labels:
        app: legend-studio
    spec:
      containers:
      - name: legend-studio
        image: finos/legend-studio:3.0.0
        ports:
        - containerPort: 8080
        env:
        - name: LEGEND_STUDIO_PORT
          value: "8080"
        - name: LEGEND_STUDIO_HOST
          value: "0.0.0.0"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: legend-studio
  namespace: legend-prod
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: legend-studio