# Azure Legend Platform Configuration
# This file contains the Azure-specific configuration for Legend services
# Update the values after running the Azure deployment script

# Azure Resource Configuration
AZURE_RESOURCE_GROUP=rs-finos-legend
AZURE_AKS_CLUSTER=aks-legend
AZURE_ACR_NAME=acrlegend[UNIQUE_SUFFIX]
AZURE_ACR_LOGIN_SERVER=acrlegend[UNIQUE_SUFFIX].azurecr.io

# Legend Service URLs (Update with your Azure endpoints)
LEGEND_ENGINE_URL=https://legend-engine.azure-legend.com
LEGEND_SDLC_URL=https://legend-sdlc.azure-legend.com
LEGEND_STUDIO_URL=https://legend-studio.azure-legend.com

# Azure MongoDB Configuration
MONGODB_URI=mongodb://admin:[PASSWORD]@mongo-legend-[UNIQUE_SUFFIX].mongo.cosmos.azure.com:10255/legend?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@mongo-legend-[UNIQUE_SUFFIX]@

# GitLab OAuth Configuration (Update redirect URIs for Azure)
GITLAB_APP_ID=29713286
GITLAB_APP_SECRET=gloas-81d50924397a5775489f205099fc17f82a84ae41557739099313383b46ab05ab
GITLAB_HOST=gitlab.com

# Azure-specific OAuth Redirect URIs (Update these in GitLab)
# Go to GitLab > User Settings > Applications > Your Legend App
# Update the redirect URI to: https://legend-sdlc.azure-legend.com/api/pac4j/login/callback

# Production Configuration
NODE_ENV=production
LOG_LEVEL=INFO

# Azure Container Registry Authentication
AZURE_ACR_USERNAME=[ACR_USERNAME]
AZURE_ACR_PASSWORD=[ACR_PASSWORD]

# Kubernetes Namespace
K8S_NAMESPACE=legend-system

# TLS Configuration
ENABLE_TLS=true
TLS_CERT_EMAIL=your-email@domain.com
TLS_CERT_DOMAIN=legend.azure-legend.com
